#include <stdio.h>

int main()
{

        // declare um loop for que repita de 0 a 9, incrementando de 1.
    int i = 0;
    //for (int i = 0; i < 10; i++)
    //{

        // Dentro do loop for, insira uma chamada à função printf() para imprimir a variável i.
        //printf("%i ", i);
        // Declarar um loop for que repita de 9 a 0, decrementando de 1.
       // for(int i=9; i>=0; i--) {
           // printf("%i ", i);
       //
       // }
// Insirir uma chamada à função printf() para imprimir a variável i.  
// declarar uma loop for que execute infinitamente .
for(;;) {
    // Insirir uma chamada à função printf() para imprimir a mensagem “Ao infinito e alem!”
    printf("Ao infinito e alem!\n");

}

    
        return 0 ;
}
